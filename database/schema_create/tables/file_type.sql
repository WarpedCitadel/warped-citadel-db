-- ===========================================
-- create file_type
-- ===========================================
CREATE TABLE file_type (
  id 				SERIAL NOT NULL PRIMARY KEY,
  file_type_name 	BIGINT NOT NULL
);
