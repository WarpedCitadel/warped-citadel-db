-- ===========================================
-- create status_type
-- ===========================================
CREATE TABLE status_type (
  id 				SERIAL NOT NULL PRIMARY KEY,
  status_type_name 	BIGINT NOT NULL
);